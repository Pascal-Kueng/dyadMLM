% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_interdep_data.R
\name{prepare_interdep_data}
\alias{prepare_interdep_data}
\title{Prepare dyadic data for interdep}
\usage{
prepare_interdep_data(
  data,
  group,
  member,
  role = NULL,
  time = NULL,
  predictors = NULL,
  outcomes = NULL,
  model_type = "apim",
  temporal_predictor_decomposition = c("auto", "time_2l", "none"),
  set_compositions_exchangeable = NULL,
  incomplete_dyads = c("error", "drop"),
  missing_role = c("error", "drop"),
  seed = NULL
)
}
\arguments{
\item{data}{A data frame or tibble. Data must be in long format. For
cross-sectional dyadic data, each observed member of each dyad has one row.
For intensive longitudinal dyadic data, each observed member of each dyad has
one row per observed time point.}

\item{group}{Column identifying the dyad.}

\item{member}{Column identifying a person or the member within dyad.}

\item{role}{Optional column identifying a stable member role, such as gender.
Values must be stable within each \code{group} x \code{member} and must not contain
\verb{_x_}. Missing role information is controlled by \code{missing_role}. If no role
is supplied, all dyads are treated as the same type of exchangeable dyads.}

\item{time}{Optional column identifying time or measurement order of repeated
measures.}

\item{predictors}{Optional variables to use for temporal predictor
decomposition and model-ready predictor construction.}

\item{outcomes}{Optional variables to use for model-ready outcome construction.
Currently only needed for \code{model_type = "undirected_dsm"}.}

\item{model_type}{Model-ready column families to construct. Can contain one
or more of \code{"apim"}, \code{"dim"}, and \code{"undirected_dsm"}. \code{"apim"} creates
actor and partner predictors. \code{"dim"} creates dyad-mean and
within-dyad-deviation predictors. \code{"undirected_dsm"} creates undirected
dyadic-score model columns. \code{"none"} skips model-specific predictor and
outcome construction after validation, composition inference, and optional
temporal predictor decomposition, and must be used alone.}

\item{temporal_predictor_decomposition}{Temporal decomposition strategy for
\code{predictors}.
\code{"none"} leaves predictors undecomposed before model-specific columns are
constructed. \code{"time_2l"} indicates a two-level temporal predictor
decomposition into within-person and between-person components. \code{"auto"}
resolves to \code{"time_2l"} when both \code{time} and \code{predictors} are supplied, and
to \code{"none"} otherwise. Raw cross-sectional DIM predictor dyad-mean columns
are still centered around the grand mean of dyad means as part of DIM-style
predictor construction. For longitudinal DIM or undirected DSM predictor
construction, raw undecomposed predictors are currently rejected; use
\code{"auto"} or \code{"time_2l"}.}

\item{set_compositions_exchangeable}{Optionally specify dyad compositions
to treat as exchangeable, when their roles would otherwise make them
distinguishable. Requires \code{role}; compositions that are already
exchangeable should not be listed. Each composition must be supplied as one
string, using \verb{_x_}, \code{-}, \verb{_}, or whitespace between the two role labels,
for example \code{"female_x_male"}, \code{"female-male"}, \code{"female_male"}, or
\code{"female male"}. To set multiple compositions, use a character vector of
such strings.}

\item{incomplete_dyads}{How to handle dyads that do not contain exactly two
unique members anywhere in the data. \code{"error"} stops with an error and
\code{"drop"} removes the entire dyad.}

\item{missing_role}{How to handle missing values in the \code{role} column.
\code{"error"} stops with an error, \code{"drop"} removes dyads with incomplete role
information. Ignored when no \code{role} column is supplied.}

\item{seed}{Optional seed for random \verb{.i_diff_*} sign assignment in
exchangeable dyads. If \code{NULL}, the current R session's RNG state is used.}
}
\value{
The original data as a tibble with class \code{interdep_data},
\code{.i_composition} and \code{.i_composition_role} factor columns,
\verb{.i_is_*} numeric indicator columns, composition-specific
\verb{.i_diff_*} columns for exchangeable dyads, and an \code{interdep} attribute
containing structural metadata, \code{dyad_compositions}, and predictor metadata
such as \code{temporal_predictor_decompositions}, \code{apim_predictors}, and
\code{dim_predictors} when applicable.
}
\description{
Validates dyadic data, records the structural variables, and adds metadata
used by downstream interdep functions.
}
\details{
Data must be in long format. Cross-sectional dyadic data may contain at most
one row per member within dyad. Intensive longitudinal dyadic data may
contain at most one row per member and observed measurement occasion within
dyad. Measured variables may contain missing values. Missing or incomplete
structural information is controlled by \code{incomplete_dyads} and
\code{missing_role}.

Dyad composition labels are canonical: role labels are sorted alphabetically
before being combined, so labels do not depend on row or member order.
}
\examples{
data <- data.frame(
  dyad_id = c(1, 1, 2, 2),
  person_id = c(1, 2, 3, 4),
  role = c("female", "male", "female", "male")
)

prepared <- prepare_interdep_data(
  data,
  group = dyad_id,
  member = person_id,
  role = role
)

attr(prepared, "interdep")$dyad_compositions

print(prepared)
}
