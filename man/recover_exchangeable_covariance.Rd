% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtransform_residual_covariance.R
\name{recover_exchangeable_covariance}
\alias{recover_exchangeable_covariance}
\title{Recover member-level residual covariance from exchangeable random-effect blocks}
\usage{
recover_exchangeable_covariance(model, block_pairings = NULL)
}
\arguments{
\item{model}{A fitted \code{glmmTMB} or single-response \code{brmsfit} model. For
\code{glmmTMB}, only conditional-model random effects are processed.}

\item{block_pairings}{\code{NULL} (default) for automatic block matching. Otherwise, supply
one block-pair specification or a list of block-pair specifications. Each
pair contains:
\itemize{
\item \code{shared_block}: a single string naming the shared random-effect term
copied from the fitted model formula or an equivalent selector (see
Details), or \code{NULL} if the entire shared block was omitted when fitting;
\item \code{difference_block}: a single string naming the member-difference random-effect
term or an equivalent selector, or \code{NULL} if the entire difference block
was omitted;
\item \code{difference_indicator}: the exact name of the difference-indicator
column used in \code{difference_block}. It is required when \code{difference_block}
selects a block and optional when \code{difference_block = NULL};
\item \code{shared_indicator}: the exact shared composition-indicator column,
needed only for composition-specific blocks in mixed-dyad models. It
defaults to \code{"1"}, meaning that every fitted row belongs to the pair and
an ordinary intercept is the shared intercept coordinate.
}}
}
\value{
An \code{exchangeable_rescov} object: a named list with one element per
matched block pairing. Each element contains \code{shared_term} and
\code{difference_term} (each a fitted term string or \code{NULL}), plus the member-level
variance-covariance matrix in \code{varcov} and its standard-deviation/correlation
representation in \code{sdcor}, with standard deviations on the diagonal and
correlations off the diagonal. Covariance dimension labels \code{member_1} and
\code{member_2} denote arbitrary exchangeable positions, not substantive roles
or encodings. Caller-supplied outer names are preserved; unnamed pairings
receive stable names \code{pair_1}, \code{pair_2}, and so on in resolved order.
For \code{glmmTMB}, \code{varcov} and \code{sdcor} are matrices. For \code{brms}, they
are posterior-draw by coefficient by coefficient arrays.
}
\description{
Back-transforms covariance matrices from paired shared and member-difference
random-effect blocks to the covariance structure of two exchangeable
members. The result is on the fitted random effects' linear-predictor scale.
In non-Gaussian models, it therefore describes a Gaussian latent covariance,
not response-scale residual covariance. For the model specification,
derivation, and interpretation, see the
\href{https://pascal-kueng.github.io/dyadMLM/articles/apim.html#exchangeable-residual-structure}{exchangeable APIM vignette}.
}
\details{
Automatic matching recognizes exact \verb{.dy_member_contrast_*_arbitrary} and
legacy \verb{.dy_diff_*_arbitrary} coefficient names
and first looks for the corresponding \verb{.dy_is_*} shared block. It requires
the two blocks to use the same grouping factor and the same underlying
terms. Most models fitted with \code{dyadMLM}-generated columns therefore need
only:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{result <- dyadMLM::recover_exchangeable_covariance(model)
print(result)
}\if{html}{\out{</div>}}

Supply \code{block_pairings} when automatic matching is ambiguous or when a model uses
custom indicators, multiple covariance levels, or deliberately omitted
blocks or terms. To specify one pair with a custom difference indicator:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{result <- dyadMLM::recover_exchangeable_covariance(
  model,
  block_pairings = list(
    shared_block = "(1 + time | coupleID)",
    difference_block = "(0 + my_diff + I(my_diff * time) | coupleID)",
    difference_indicator = "my_diff"
  )
)
}\if{html}{\out{</div>}}

For multiple covariance levels, wrap the pairings in an outer list. For example,
in a Gaussian \code{glmmTMB} model fitted with \code{dispformula = ~ 0}, this call
recovers both a stable dyad-level covariance with an omitted difference
time slope and the same-occasion partner residual covariance:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{result <- dyadMLM::recover_exchangeable_covariance(
  model,
  block_pairings = list(
    dyad = list(
      shared_block = "(1 + diaryday | coupleID)",
      difference_block = "(0 + .dy_member_contrast_assumed_exchangeable_arbitrary | coupleID)",
      difference_indicator =
        ".dy_member_contrast_assumed_exchangeable_arbitrary"
    ),
    same_occasion = list(
      shared_block = "(1 | coupleID:diaryday)",
      difference_block = "(0 + .dy_member_contrast_assumed_exchangeable_arbitrary | coupleID:diaryday)",
      difference_indicator =
        ".dy_member_contrast_assumed_exchangeable_arbitrary"
    )
  )
)
}\if{html}{\out{</div>}}

At the dyad level, the fitted model includes a shared time slope but no
difference time slope. Thus, the two members' time random effects are
identical at this level, with correlation \code{+1} whenever the shared slope
variance is non-zero; at zero variance, the correlation is undefined.
Covariances involving the diary-day slope are therefore supplied entirely by
the shared block.

The random-effect terms may be copied exactly from the model formula.
Equivalent backend syntax is also recognized, such as
\code{(1 + time | group)} and \code{us(1 + time | group)}, or
\code{(0 + x || group)} and \code{diag(0 + x | group)}. A custom difference-indicator
name is supplied literally. Difference slopes may be written in either
interaction order or as a simple product inside \code{I()}.

When a difference block is supplied and the fitted model frame retains the
indicator columns, \code{difference_indicator} must assign \code{-1} and \code{+1} to the
two arbitrary member positions consistently within each dyad. For
composition-specific blocks, it must be zero where \code{shared_indicator} is
zero.

For custom difference indicators supplied through \code{block_pairings}, the function
checks whether both positions occur within each supported fitted grouping
unit. It rejects coding when no group contains both positions, and warns when
only some groups are one-sided, which can result from fitted-row filtering.
It also warns when stable member assignments cannot be verified across
repeated rows without a member identifier.
}
\section{What omitted blocks and terms mean}{

\code{recover_exchangeable_covariance()} only describes constraints that were already imposed
when the model was fitted. It does not remove a block, set a variance to zero,
or otherwise constrain the supplied model. Describe only the structure that
was actually fitted.

If a term occurs in only one selected block, the function represents the
missing coordinate as a structural zero:
\itemize{
\item A term present only in the shared block has no difference component, so
the two members have identical random effects for that term.
\item A term present only in the difference block has no shared component, so
the two members have equal-magnitude, opposite-sign random effects for
that term.
}

Setting \code{difference_block = NULL} or \code{shared_block = NULL} applies the corresponding rule
to the entire omitted block. This is valid only when that block is truly
absent from the fitted model. Do not use \code{NULL} merely to ignore an existing
block; the resulting back-transformation would be incorrect.

See the constrained-block example in the
\href{https://pascal-kueng.github.io/dyadMLM/articles/apim.html#fitted-constraints-and-omitted-blocks}{exchangeable APIM vignette}.
}

\section{Backend note}{

For \code{glmmTMB}, random effects in non-conditional components are ignored with
a warning. See the vignette's
\href{https://pascal-kueng.github.io/dyadMLM/articles/apim.html#exchangeable-random-slope-back-transformation}{extension to exchangeable random slopes}
section for the manual calculation.

In \code{brms}, cross-sectional and same-occasion partner dependence can be
represented directly with
\code{unstr(time = member_position, gr = residual_group)}. With Gaussian
outcomes, \code{sigma ~ 1} supplies the common residual scale. Non-Gaussian
families have no \code{sigma} parameter here; \code{unstr()} instead estimates a
common latent residual scale and correlation on the linear-predictor scale.
Here,
\code{member_position} identifies the same two arbitrary positions within every
group, and \code{residual_group} identifies dyads in cross-sectional data or
dyad-occasions in longitudinal data. This direct specification applies when
one covariance structure is sufficient. Separate composition-specific
\code{unstr()} structures for mixed dyad types are not currently supported in a
standard single-response \code{brms} model. For Gaussian mixed-dyad residual
covariance, use \code{glmmTMB}. Shared/difference blocks remain relevant for
higher-level random effects and can represent latent link-scale covariance
in non-Gaussian models.
}

\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
  example_data <- prepare_dyad_data(
    dyads_cross,
    dyad = coupleID,
    member = personID,
    role = gender,
    model_types = "none",
    # dyads_cross contains three compositions; retain `female-female` here.
    keep_compositions = "female-female",
    seed = 123
  )

  model <- glmmTMB::glmmTMB(
    closeness ~ 1 +
      us(1 | coupleID) +
      us(0 + .dy_member_contrast_female_x_female_arbitrary | coupleID),
    dispformula = ~ 0,
    data = example_data
  )

  recover_exchangeable_covariance(model)
}

}
\seealso{
The
\href{https://pascal-kueng.github.io/dyadMLM/articles/apim.html#exchangeable-residual-structure}{exchangeable APIM vignette}
for the model specification, covariance derivation, and constrained-block
example. Run \code{vignette("apim", package = "dyadMLM")} to open the installed
version.
}
