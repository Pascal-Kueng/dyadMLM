% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_interdep_data.R
\name{validate_interdep_data}
\alias{validate_interdep_data}
\title{Validate dyadic input data}
\usage{
validate_interdep_data(
  data,
  group,
  member,
  role = NULL,
  time = NULL,
  predictors = NULL,
  model_type = c("apim", "dim", "apim_dim", "none"),
  temporal_decomposition = c("auto", "time_2l", "none"),
  incomplete_dyads = c("error", "drop"),
  missing_role = c("error", "drop")
)
}
\arguments{
\item{data}{A long-format data frame or tibble.}

\item{group}{Column identifying the dyad.}

\item{member}{Column identifying the two people or members within each dyad,
such as a person ID.}

\item{role}{Optional column identifying role that can be used to distinguish
partners within a dyad, such as gender. If no role is supplied, all dyads
in the data are treated as exchangeable.}

\item{time}{Optional column identifying time or measurement order.}

\item{predictors}{Optional variables to select and store as metadata for
temporal decomposition and model-helper functions.}

\item{model_type}{Requested predictor shape for downstream construction.
\code{"apim"} indicates actor and partner predictors, \code{"none"} indicates no
model-specific predictor construction, \code{"dim"} indicates dyad-mean and
within-dyad-deviation predictors, and \code{"apim_dim"} indicates both APIM and
DIM predictors.}

\item{temporal_decomposition}{Requested predictor temporal-decomposition
strategy for downstream construction. \code{"none"} leaves predictors
undecomposed before model-specific columns are constructed. \code{"time_2l"}
indicates a two-level temporal decomposition into within-person and
between-person predictor components. \code{"auto"} resolves to \code{"time_2l"} when
both \code{time} and \code{predictors} are supplied, and to \code{"none"} otherwise.
Model-specific helpers may apply additional conventions, such as grand-mean
centering raw cross-sectional DIM dyad means.}

\item{incomplete_dyads}{How to handle dyads that do not contain exactly two
unique members anywhere in the data. \code{"error"} stops with an error and
\code{"drop"} removes the entire dyad.}

\item{missing_role}{How to handle missing values in the \code{role} column.
\code{"error"} stops with an error, \code{"drop"} removes dyads with incomplete role
information. Ignored when no \code{role} column is supplied.}
}
\value{
A tibble with class \code{interdep_data} and metadata about the dyad,
member, optional role, and optional time columns.
}
\description{
Checks whether \code{data} has a valid long-format dyadic structure and returns it
as a tibble with an additional \code{interdep_data} class. Cross-sectional data
may contain at most one row per member within each dyad. Intensive
longitudinal data may contain at most one row per member and measurement
occasion within each dyad.
}
\keyword{internal}
