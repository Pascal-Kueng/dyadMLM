% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_undirected_dsm_columns.R
\name{add_undirected_dsm_columns}
\alias{add_undirected_dsm_columns}
\title{Add dyadic-score model (undirected) predictor and outcome columns}
\usage{
add_undirected_dsm_columns(data)
}
\arguments{
\item{data}{An \code{interdep_data} object returned by \code{\link[=prepare_interdep_data]{prepare_interdep_data()}}.}
}
\value{
An \code{interdep_data} object with dyad-mean and within-dyad-deviation
predictor columns added and DSM predictor metadata recorded.
}
\description{
Adds Dyad-Score Model (DSM) style dyad-mean and within-dyad-deviation
columns for the predictors and outcomes recorded in an \code{interdep_data} object. For
currently supported non-directed DSMs, \code{.i_diff} must be nonzero for every
retained row. This means distinguishable dyads are not supported by DSM
construction until explicit role-contrast or pooling support is added.
Predictors are constructed and treated identically do the DIM method. The outcome
is treated the same as cross-sectional raw DIM predictors and is not temporally
decomposed. Instead, a raw dyad mean and difference is created at each time-point.
}
\details{
The function reads \code{attr(data, "interdep")$temporal_predictor_decompositions} and
stores the constructed DSM columns in
\code{attr(data, "interdep")$undirected_dsm_predictors}.
}
\keyword{internal}
